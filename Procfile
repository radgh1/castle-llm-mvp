# Railway configuration for Node.js backend

# Start command - Railway will use this to start your app
# The tsx package allows us to run TypeScript directly
start="npx tsx --no-warnings server/src/index.ts"
